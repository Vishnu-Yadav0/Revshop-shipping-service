spring:
  application:
    name: shipping-service
  datasource:
    url: jdbc:mysql://localhost:3306/revshop_shipping
    username: rev_user
    password: P@ssw0rd!
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 8086
  servlet:
    context-path: /api/shipping

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

logging:
  level:
    com.revature.revshop: DEBUG
